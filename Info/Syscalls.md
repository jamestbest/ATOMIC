# Compiler Outputted Calls to Kernel

within the compiler there will be some basic functions that have OS independent functions
e.g. read/write to a file with some parameters.

These will be bytecode level

When further compiled to the OS specific functions will be added per OS